#include <iostream>

using namespace std;
// #include <opencv2/opencv.hpp>

int main(){
    // cv::Mat a = cv::imread("tpl_match.png", 1); 
    // cv::imshow("hello", a); 
    // cv::waitKey();
    cout<< "hello"; //<<endl;
    return 0;
}